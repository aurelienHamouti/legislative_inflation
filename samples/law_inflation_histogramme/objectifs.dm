Objectifs (d3 v6)

    importer les données
    les convertir au bon format (parseTime)
    définir des échelles (d3.scaleLinear et d3.timeScale et non d3.scale.linear v4!!!)

    comprendre la différence entre domain (données) et range (rendu)

    créer des rectangles en fonction des données et des échelles

    avec la fonction d3.bin (et non d3.histogram v5!!!)

    créer des axes à partir des échelles
    ajouter ces axes au graphique